// Imports JSON data
define(["../../common/data"], function (data) {

    data("operation").on("save", function (operation) {
        if (operation.type == "import") {
            
        }
    });
});
// Emitter system, emits a specific type of object in a set direction, speed, duration, etc
define(["data"], function (data) {

    // Every frame
    data("frame").on("save", function(frame) {

        // Iterate over each emitter
        data("emitter").each(function(motion) {

        });
    });
});
// This will kill an actor which reaches 0 health.
define(["core/common/data"], function (data) {
    
    var world = data("world");
    var actors = data("actor");
    var frames = data("frame");

    world.on("save", function (frame) {
        
    });
});